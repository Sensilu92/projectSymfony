<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Symfony project{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" type="text/css" media="screen" href="{{ asset('bundles/css/css.css') }}" />
    </head>
    <body>
      
        {% block body %}  
            {% block menu %} 
                <div class="menu">
                        
                    {% if app.session.get('role') == 'admin' %}
                        <a href="{{ path('suiviprojet_administrateur_creation_compte_client') }}">Création compte client</a>
                        <a href="{{ path('suiviprojet_administrateur_creation_projet') }}">Création projet</a>
                        <a href="{{ path('sprint_assignation_user_stories') }}">Assignation user stories</a>
                        <a href="{{ path('sprint_creation') }}">Création sprint</a>
                    {% endif%} 
                        
                        <a href="{{ path('suivi_projet_developper_creation_notice') }}">Création notice d'utilisation</a>
                   {# {% elseif app.session.get('role') == 'dev'%}#}
                        <a href="{{ path('suivi_projet_developper_cra') }}">CRA</a>
                        <a href="{{ path('sprint_tableau_de_bord_sprint') }}">Tableau de bord sprint</a>
                        
                    {#{% endif %}#}
                        
                    <a href="{{ path('blacklog_product_creation_userstories') }}">User storie</a>
                    <a href="{{ path('suiviprojet_administrateur_deconnexion') }}">Deconnexion</a>
                    {#{% endif%}#}
                        
                </div> 
            {% endblock %}
           
        {% endblock %}
        
        {% block javascripts %}{% endblock %}
        
    </body>
    
</html>